interface:
  display_name: "INIAD MOOCs"
  short_description: "Operate INIAD MOOCs through the imoocs CLI."
  default_prompt: "Use $imoocs and the local imoocs CLI to handle the requested INIAD MOOCs task."
