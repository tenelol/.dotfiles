# Ghostty config
# See: https://ghostty.org/docs/config

# Typography
font-family = "JetBrains Mono"
font-size = 13
font-feature = "calt"

# Window feel
window-padding-x = 14
window-padding-y = 12
window-decoration = "none"
window-show-tab-bar = "never"
# Increase opacity to keep glyphs legible; drop blur to avoid haloing
background-opacity = 0.9
background-blur = 0

# Cursor
cursor-style = "block"

# Colors
# Keep default text colors; rely on background-opacity for transparency.

# Use a context menu on right-click.
right-click-action = "context-menu"
