# Monitors aligned with niri config
# External 4K (DP-3) primary at origin, 200% scale
monitor = DP-3, 3840x2160@75, 0x0, 2
# Secondary (HDMI-A-1) to the right; align with DP-3 logical width (3840/2)
monitor = HDMI-A-1, 1920x1080@165, 1920x0, 1
# Fallback for laptop panel when others are absent
monitor = eDP-1, preferred, 0x0, 1
# Catch-all fallback
monitor = , preferred, auto, 1
