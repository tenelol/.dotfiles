# Monitors aligned with niri config
# External 4K (DP-3) primary at origin, 200% scale
monitor = DP-3, 3840x2160@75, 0x0, 2
# Secondary (HDMI-A-1) to the right, slight vertical offset
monitor = HDMI-A-1, 1920x1080@165, 3840x80, 1
# Fallback for laptop panel when others are absent (slightly larger scale)
monitor = eDP-1, preferred, 0x0, 2
# Catch-all fallback
monitor = , preferred, auto, 1
