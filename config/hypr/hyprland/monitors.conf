# Monitors aligned with niri config
# Secondary (HDMI-A-1) on the left
monitor = HDMI-A-1, 1920x1080@165, 0x0, 1
# External 4K (DP-3) primary on the right; offset by HDMI width
monitor = DP-3, 3840x2160@75, 1920x0, 2
# Fallback for laptop panel when others are absent
monitor = eDP-1, preferred, 0x0, 1
# Catch-all fallback
monitor = , preferred, auto, 1
